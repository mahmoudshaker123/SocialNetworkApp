# SocialNetworkApp
A social network app using Django would allow users to create and edit profiles, connect with other users, view a newsfeed with updates from connected users, post and share content, search for users, send private messages, and be managed by admins. 
